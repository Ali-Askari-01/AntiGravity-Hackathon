pythonRuntime = "3.12"

[phases.setup]
nixPkgs = ["...", "gcc"]

[phases.build]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}"